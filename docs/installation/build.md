# Building from source
You can install shaweelTimer by building from source.

You usually don't actually build the application, you only do that on Windows with `pyinstaller` you mostly just `git clone` the repository and make a .desktop file for the program.
