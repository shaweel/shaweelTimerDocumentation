# Building from source
You can install shaweelTimer by building from source.

Do not do this on [Windows], instead follow [Windows.md](windows.md). Only do this on [GNU]+[Linux]

You usually don't actually build the application, you mostly just `git clone` the repository and make a .desktop file for the program.

One requirement is that you will **NEED** to run the generateVersionData.sh script and 
